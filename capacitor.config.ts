import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.lucrodriver.app',
  appName: 'lucrodriver',
  webDir: 'artifacts/driver-metrics/dist/public'
};

export default config;